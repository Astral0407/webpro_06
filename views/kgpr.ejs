<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>カゲロウプロジェクト</title>
</head>
<body>
  <h2>カゲプロキャラ表一覧</h2>

  <table border="1">
    <tr>
      <th>ID</th>
      <th>団員No</th>
      <th>名前</th>
      <th>操作</th>
    </tr>

    <% data.forEach((row, index) => { %>
      <tr>
        <td><%= row.id %></td>
        <td><%= row.number %></td>
        <td><a href="/kgpr/<%= index %>"><%= row.name %></a></td>

        <!-- 編集と削除の新しい列 -->
        <td>
          <a href="/kgpr/edit/<%= index %>">編集</a>
          |
          <a href="/kgpr/delete/<%= index %>">削除</a>
        </td>
      </tr>
    <% }) %>
  </table>

  <br>
  <a href="/kgpr/create">追加</a>
</body>
</html>